apiVersion: v1
kind: Service
metadata:
  name: medicine-service
  namespace: front
spec:
  selector:
    app: medicine-front
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: NodePort
